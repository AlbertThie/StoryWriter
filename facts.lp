#const beatsnumber=3.
#const peak=2.
#const suspensepeak=50.


agent(ben,hero).
agent(sil,villain).
location(seedyBar).
location(office).
beat(0,empty,empty,empty,empty,0).
beats(beatnumber).

at(ben,office,0).
at(sil,seedyBar,0).